import { OrderDaysPipe } from './order-days.pipe';

describe('OrderDaysPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderDaysPipe();
    expect(pipe).toBeTruthy();
  });
});
